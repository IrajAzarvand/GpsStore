import 'package:flutter/material.dart';

class LiveMapPage extends StatelessWidget {
  const LiveMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('نقشه زنده'),
      ),
      body: const Center(
        child: Text('این بخش در حال توسعه است.'),
      ),
    );
  }
}
