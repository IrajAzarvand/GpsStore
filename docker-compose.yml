services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: ${DATABASE_NAME}
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backup/postgres:/backup
    ports:
      - "5432:5432"
    networks:
      - GpsStore

  redis:
    image: redis:7-alpine
    restart: always
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - GpsStore

  web:
    build: .
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env.production
    environment:
      DEBUG: ${DEBUG}
      SECRET_KEY: ${SECRET_KEY}
      DATABASE_ENGINE: ${DATABASE_ENGINE}
      DATABASE_NAME: ${DATABASE_NAME}
      DATABASE_USER: ${DATABASE_USER}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD}
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      REDIS_URL: redis://redis:6379/1
      REDIS_HOST: redis
      ALLOWED_HOSTS: ${ALLOWED_HOSTS}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      EMAIL_BACKEND: ${EMAIL_BACKEND}
      EMAIL_HOST: ${EMAIL_HOST}
      EMAIL_PORT: ${EMAIL_PORT}
      EMAIL_USE_TLS: ${EMAIL_USE_TLS}
      EMAIL_HOST_USER: ${EMAIL_HOST_USER}
      EMAIL_HOST_PASSWORD: ${EMAIL_HOST_PASSWORD}
      DEFAULT_FROM_EMAIL: ${DEFAULT_FROM_EMAIL}
      SENTRY_DSN: ${SENTRY_DSN}
      ENVIRONMENT: production
      DJANGO_SUPERUSER_USERNAME: ${DJANGO_SUPERUSER_USERNAME}
      DJANGO_SUPERUSER_PASSWORD: ${DJANGO_SUPERUSER_PASSWORD}
      DJANGO_SUPERUSER_EMAIL: ${DJANGO_SUPERUSER_EMAIL}
    volumes:
      - ./media:/app/media
      - ./staticfiles:/app/staticfiles
      - ./logs:/app/logs
    depends_on:
      - db
      - redis
    networks:
      - GpsStore

  gps_receiver:
    build: .
    restart: always
    command: ["python", "manage.py", "gps_receiver"]
    ports:
      - "5000:5000"
      - "5000:5000/udp"
    env_file:
      - .env.production
    environment:
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      REDIS_URL: redis://redis:6379/1
      REDIS_HOST: redis
      ENVIRONMENT: production
    volumes:
      - ./media:/app/media
      - ./staticfiles:/app/staticfiles
      - ./logs:/app/logs
    depends_on:
      - db
      - redis
    networks:
      - GpsStore

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/sites-enabled:/etc/nginx/sites-enabled
      - ./ssl:/etc/nginx/ssl
      - ./staticfiles:/app/staticfiles
      - ./media:/app/media
    depends_on:
      - web
    networks:
      - GpsStore

  # Nominatim (Self-hosted Reverse Geocoding)
  # To enable local hosting for Iran maps:
  # 1. Uncomment the service below
  # 2. Setup requires initial import (takes time/resources)
  # 3. Update settings.py: NOMINATIM_BASE_URL=http://nominatim:8080/reverse
  # nominatim:
  #   image: mediagis/nominatim:4.4
  #   container_name: nominatim
  #   restart: always
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     # Download Iran map data automatically on first start
  #     - PBF_URL=https://download.geofabrik.de/asia/iran-latest.osm.pbf
  #     - REPLICATION_URL=https://download.geofabrik.de/asia/iran-updates
  #     - IMPORT_STYLE=address
  #     - THREADS=4
  #   volumes:
  #     - nominatim_data:/var/lib/postgresql/14/main
  #   networks:
  #     - GpsStore
  #   shm_size: 1gb

volumes:
  postgres_data:
  redis_data:
  nominatim_data:


networks:
  GpsStore:
    driver: bridge
